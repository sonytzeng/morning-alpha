// ═══════════════════════════════════════════════════════════════════
// V7.52: FALLBACK NOTEBOOK SECTIONS (9 sections, deep structure)
// ═══════════════════════════════════════════════════════════════════
function buildFallbackNotebookSections(bias:string,confScore:number,isDSI:boolean,chains:Record<string,unknown>[]):Record<string,unknown>[]{const cn=chains.length>0?`系統偵測到${chains.length}條影響鏈。`:'系統未偵測到明確影響鏈。';const ds=isDSI?'（資料不足，以保守估計為主）':'';return[{key:'main_scenario',title:'今日主劇本',conclusion:`今日盤前狀態為「${bias}」${ds}。系統正在等待更多市場數據確認方向。`,data_points:[`市場方向判定：${bias}`,`把握度：${confScore}/100`,cn],reasoning:`根據目前可獲得的隔夜市場數據與新聞，系統判定今日台股開盤方向為「${bias}」。${cn}。在資料不足的情況下，系統採取保守判定，避免過早鎖定單一方向。建議會員以09:00開盤方向為主要參考點，而非將盤前假設視為確定結論。`,market_mechanism:'隔夜美股指數與ADR價格變化會透過外資期貨部位與台積電ADR換算價差影響台股開盤，但開盤後仍需觀察現貨與期貨是否同步確認方向。',intraday_confirmation:['09:00-09:15：觀察台積電開盤是否與盤前假設方向一致','09:30：觀察半導體與AI伺服器族群是否出現同步擴散'],invalidation_conditions:['若台積電開盤方向與盤前假設相反，立即降權盤前判斷','若台指期與加權指數不同步超過15分鐘，代表盤前訊號未被確認'],member_takeaway:'今天不要急著在開盤前就決定方向。先確認台積電與台指期是否同步，再依盤中數據調整。盤前假設只是起點，不是終點。'},{key:'evidence',title:'今天判斷根據什麼',conclusion:`系統根據隔夜美股數據、台指期夜盤與市場新聞進行盤前方向判定，把握度${confScore}/100。`,data_points:['隔夜美股期貨變化','台積電ADR價格變動','SOX費半指數趨勢'],reasoning:`系統判定「${bias}」的主要依據來自隔夜市場數據。如果NVDA與SOX同步上漲，半導體與AI伺服器族群可能受到正向傳導。如果VIX同步上升或DXY走強，則需留意風險資產壓力。目前把握度為${confScore}/100。`,market_mechanism:'台積電ADR價格通常比台股台積電現貨提前反應美股投資人對半導體的情緒，兩者價差若擴大，台股開盤時台積電現貨會有補漲或補跌空間。',intraday_confirmation:['觀察台積電是否強於TAIEX（代表半導體資金集中）','觀察AI伺服器族群（廣達/緯創/緯穎）是否同步表態'],invalidation_conditions:['如果NVDA/TSM ADR的漲幅沒有傳導到台股半導體族群（09:30仍未跟進），代表傳導失效','如果VIX同步上升且DXY走強，盤前偏多假設需重新評估'],member_takeaway:'隔夜數據只是起點。09:00開盤後確認台積電與半導體族群是否真的跟進，才是判斷今天方向的關鍵。'},{key:'overnight_impact_chain',title:'隔夜影響鏈',conclusion:cn+'系統將在獲取更多數據後更新影響鏈分析。',data_points:['隔夜美股走勢','台指期夜盤變化','相關新聞事件'],reasoning:`隔夜影響鏈分析需要至少3條以上具備完整因果關係的鏈條。目前系統可用的數據中，${cn}。系統會持續追蹤市場變化，並在後續盤中更新中提供更詳細的影響鏈分析。`,market_mechanism:'美股到台股的傳導路徑：美股科技股變動→外資期貨部位調整→台積電ADR價差變化→台股開盤方向；同時VIX/DXY/US10Y的變化會影響風險資產偏好。',intraday_confirmation:['09:00：確認外資期貨未平倉量變化','10:30：確認半導體與AI族群是否有資金持續流入'],invalidation_conditions:['若美股科技股強但台股半導體開盤弱（台積電弱於大盤），代表傳導失效','若外資期貨部位轉空且現貨賣超，影響鏈方向需反轉'],member_takeaway:'影響鏈不是線性公式。海外強不等於台股強，關鍵在於傳導過程中台積電與外資期貨是否同步確認。'},{key:'do_not_do',title:'今日不要做清單',conclusion:`今日在「${bias}」的盤前狀態下，以下行為應避免。`,data_points:['市場方向：'+bias,'把握度：'+confScore+'/100'],reasoning:`在${bias}且把握度${confScore}/100的狀態下，最常見的錯誤是把盤前假設當成確定結論。如果盤前假設為偏多而開盤後台積電弱於TAIEX，代表盤前判斷需要修正。`,market_mechanism:'開盤前15分鐘通常是隔夜資訊消化期，方向尚未穩定。此時急著進場最容易追在日內高點或低點。',intraday_confirmation:['09:15後確認方向穩定再評估','09:30後確認族群擴散再考慮'],invalidation_conditions:['若台積電開盤方向與盤前假設相反且15分鐘未修正，盤前假設失效'],member_takeaway:'最危險的不是看錯方向，是把盤前假設當結論。今天先觀察09:00-09:15的方向再動作。'},{key:'watchlist',title:'今日觀察名單',conclusion:'核心觀察：台積電(2330)、聯發科(2454)、廣達(2382)；次級觀察：富邦金(2881)、緯創(3231)。',data_points:['台積電(2330)：權值股龍頭','聯發科(2454)：IC設計代表','廣達(2382)：AI伺服器代表'],reasoning:'今日核心觀察以半導體與AI伺服器族群為主。台積電的方向決定大盤方向，聯發科反映科技股資金情緒，廣達反映AI需求。如果這三檔方向一致，可信度較高。',market_mechanism:'台積電佔加權指數約30%權重，其走勢直接影響大盤方向。聯發科作為第二大權值股，反映外資對科技股的配置意願。',intraday_confirmation:['09:00：確認台積電開盤方向','09:30：確認聯發科與廣達是否與台積電同步'],invalidation_conditions:['台積電弱於大盤且聯發科不同步→多方結構需重新評估','廣達開高走低且無買盤承接→AI族群動能不足'],member_takeaway:'今天先看台積電。如果台積電強於大盤，代表資金集中半導體，AI族群可供觀察；如果台積電弱於大盤，代表外資可能偏保守。'},{key:'intraday_tracking',title:'盤中追蹤計畫',conclusion:`今日盤中追蹤以「${bias}」為基準，四個時間點依序確認。`,data_points:['09:00-09:15：開盤消化期','09:30：族群方向確認','10:30：資金持續性判斷','13:00前：收盤前調整'],reasoning:'09:00-09:15是隔夜資訊消化期，重點看台積電方向與盤前假設是否一致。09:30進入穩定階段，確認半導體與AI族群是否擴散。10:30判斷資金是否集中還是全面發散。13:00前確認是否需要修正盤前假設。',market_mechanism:'台股開盤前15分鐘常有隔夜委託單消化，09:15後方向較穩定。09:30後法人資金開始進場。10:30為盤中關鍵轉折時間點。',intraday_confirmation:['09:15：確認台積電與台指期方向一致','10:30：確認半導體族群是否擴散至AI伺服器與PCB'],invalidation_conditions:['09:30前台積電方向與盤前相反→立即降權','10:30後資金只集中在1-2檔權值股→族群擴散不足'],member_takeaway:'四個時間點各有不同任務：09:00確認方向、09:30確認族群、10:30確認持續性、13:00前決定是否修正。不用每個時間點都動作，但要知道每個時間點要看什麼。'},{key:'invalidation',title:'失效條件',conclusion:`若以下任一條件觸發，今日「${bias}」的盤前假設將被視為失效。`,data_points:['條件一：台積電開盤方向與盤前假設相反','條件二：台指期與加權指數不同步超過15分鐘','條件三：VIX/DXY同時上升'],reasoning:'失效條件是盤前判斷的安全機制。如果台積電開盤方向與盤前假設相反且09:15仍未修正，代表盤前判斷基礎可能需要全面重新評估。如果台指期與加權指數不同步，代表市場共識尚未形成。',market_mechanism:'台積電的方向是台股的領先指標。台指期反映法人對當日方向的預期。若兩者同時與盤前假設矛盾，盤前判斷的可靠性大幅降低。',intraday_confirmation:['09:15：確認台積電方向是否已修正','09:30：確認期現貨是否已同步'],invalidation_conditions:['台積電與盤前假設方向相反且09:15未修正→全面降權','VIX與DXY同時上升且台股開低→風險資產全面承壓'],member_takeaway:'失效條件不是看衰，是保護。知道什麼情況代表盤前判斷錯了，比死守盤前假設更重要。'},{key:'close_review',title:'收盤驗證與明日修正',conclusion:'收盤驗證將在今日收盤後自動產生，屆時會比對盤前假設與實際收盤結果。',data_points:['盤前假設：'+bias,'目前狀態：等待收盤'],reasoning:`收盤驗證是提升盤前判斷品質的核心機制。每天收盤後，系統會自動比對盤前假設（${bias}）與實際收盤結果，判斷：盤前假設是否方向一致、哪裡判斷正確、哪裡過於保守或積極、下一個交易日需要如何修正。`,market_mechanism:'收盤驗證透過比對盤前假設與收盤結果，建立可追蹤的判斷品質記錄。連續多日方向偏離代表盤前判斷模型需要調整參數或權重。',intraday_confirmation:['收盤後確認TAIEX實際方向','收盤後確認族群輪動是否符合盤前預期'],invalidation_conditions:['若連續3日收盤方向與盤前假設偏離，需檢視判斷模型'],member_takeaway:'收盤後記得回來看驗證結果。知道今天哪裡判斷對、哪裡需要修正，明天才會比今天更準。'},{key:'validation_feedback_adjustment',title:'最近驗證回饋與今日修正',conclusion:'目前尚無足夠收盤驗證回饋累積。系統會在使用者累積足夠交易日後自動產生。',data_points:['驗證樣本：累積中','修正方向：以當日盤前假設為主'],reasoning:'驗證回饋機制需要至少5個交易日的收盤數據才能產生意義的統計分析。目前系統正在累積樣本，後續會自動分析：盤前假設方向準確率、哪些情況下系統傾向過度樂觀或過度保守。',market_mechanism:'收盤驗證回饋透過統計盤前假設與實際收盤的偏差，自動調整後續盤前判斷的權重。',intraday_confirmation:['觀察今日盤前假設與09:00實際開盤是否一致','觀察今日盤中是否出現與盤前假設矛盾的訊號'],invalidation_conditions:['若連續多日方向偏差，系統將自動標記需人工檢視'],member_takeaway:'驗證回饋是提升判斷品質的關鍵。每天收盤後比對盤前假設與實際結果，長期下來你會看到系統的判斷品質軌跡。'}]}
function fallbackMemberReading(bias:string,chains:Record<string,unknown>[],brief:NewsBrief,score:MarketDataScore):Record<string,unknown>{const ts=Object.entries(brief.sectorCounts).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([s])=>s);const rt=score.riskReasons.length>0?score.riskReasons.join('、'):'暫無明確風險';const pt=score.reasons.length>0?score.reasons.join('、'):'訊號中性';const cs=`今日盤前市場狀態為「${bias}」。主要觀察方向：${ts.length>0?ts.join('、')+'等族群':'大盤整體方向'}。正面訊號：${pt}。風險提醒：${rt}。${chains.length>0?`隔夜影響鏈共${chains.length}條。`:'今日建議以觀察為主。'}本判讀為AI備援內容，僅供參考。`;return{core_script:cs,what_not_to_do:['不要只因單一美股訊號就判斷台股方向。','不要在數據未確認前急著追價。','不要把盤前假設當成已成立的盤中結論。'],watchlist_logic:[`重點觀察${ts.length>0?ts[0]||'大盤':'大盤'}族群方向`,'注意權值股是否帶量表態','盤中確認族群輪動是否出現明確主線'],closing_validation_focus:[`收盤後確認「${bias}」的盤前判斷是否成立`,'確認今日市場數據是否支持盤前判斷']}}

// ═══════════════════════════════════════════════════════════════════
// V7.52: normalizeNewAIFields with premium_value_summary + member_value_score
// ═══════════════════════════════════════════════════════════════════
function normalizeNewAIFields(raw:Record<string,unknown>,bias:string,confScore:number,isDSI:boolean,isNonTrading:boolean,chains:Record<string,unknown>[],closeReview:Record<string,unknown>|null,isTradingDay:boolean,dataBasis:string,latestTradingDate:string):Record<string,unknown>{const today=getTaipeiDateString();let freeSummary:Record<string,unknown>;if(raw.free_summary&&typeof raw.free_summary==='object'&&!Array.isArray(raw.free_summary)){freeSummary={...(raw.free_summary as Record<string,unknown>)};if(!freeSummary.today_status)freeSummary.today_status=isNonTrading?`非交易日回顧，最近交易日為${latestTradingDate}`:`今日盤前假設為${bias}`;if(!freeSummary.one_sentence)freeSummary.one_sentence=String(raw.summary||'今日市場觀察中。');if(!freeSummary.market_bias)freeSummary.market_bias=bias;if(freeSummary.confidence_score==null)freeSummary.confidence_score=confScore;if(!freeSummary.do_not_do)freeSummary.do_not_do='不要在資料不足時硬做方向判斷。';if(!freeSummary.mindset)freeSummary.mindset=isNonTrading?'非交易日，回顧最近盤勢節奏。':'盤前先看懂市場站哪一邊。';if(!freeSummary.cta_hint)freeSummary.cta_hint='查看完整盤前判讀'}else{freeSummary={today_status:isDSI?'今日核心資料不足，以觀察為主。':isNonTrading?`非交易日回顧，最近交易日${latestTradingDate}`:`今日盤前假設為${bias}`,one_sentence:String(raw.summary||`今日市場觀察中，盤前假設為${bias}。`),market_bias:bias,confidence_score:confScore,do_not_do:'不要在資料不足時硬做方向判斷。',mindset:isNonTrading?'非交易日，回顧最近盤勢節奏。':'盤前先看懂市場站哪一邊。',cta_hint:'查看完整盤前判讀'}}let memberResearchNote:Record<string,unknown>;if(raw.member_research_note&&typeof raw.member_research_note==='object'&&!Array.isArray(raw.member_research_note)){memberResearchNote={...(raw.member_research_note as Record<string,unknown>)};if(!memberResearchNote.generated_for_date)memberResearchNote.generated_for_date=today;if(memberResearchNote.is_trading_day==null)memberResearchNote.is_trading_day=isTradingDay;if(!memberResearchNote.data_basis)memberResearchNote.data_basis=dataBasis;if(!memberResearchNote.latest_trading_date&&latestTradingDate)memberResearchNote.latest_trading_date=latestTradingDate;const secs=memberResearchNote.sections;if(!Array.isArray(secs)||secs.length<9){memberResearchNote.sections=buildFallbackNotebookSections(bias,confScore,isDSI,chains)}}else{memberResearchNote={title:isNonTrading?'Morning Alpha 最近交易日研究筆記':'Morning Alpha 今日研究筆記',generated_for_date:today,is_trading_day:isTradingDay,data_basis:dataBasis,latest_trading_date:latestTradingDate||undefined,sections:buildFallbackNotebookSections(bias,confScore,isDSI,chains)}}const secs2=memberResearchNote.sections;if(Array.isArray(secs2)){for(const s of secs2){if(typeof s==='object'&&s!==null){const so=s as Record<string,unknown>;if(so.key==='close_review'&&typeof so.verification_result==='string'){if(hasForbiddenVerdict(so.verification_result))so.verification_result='資料不足，不做驗證';if(!VERIFICATION_RESULTS.includes(so.verification_result)){if(so.verification_result.includes('方向一致'))so.verification_result='方向一致';else if(so.verification_result.includes('保守'))so.verification_result='部分命中，盤前偏保守';else if(so.verification_result.includes('積極'))so.verification_result='部分命中，盤前偏積極';else if(so.verification_result.includes('未命中'))so.verification_result='未命中';else so.verification_result='資料不足，不做驗證'}}}}}let reelsScript:Record<string,unknown>;if(raw.reels_script&&typeof raw.reels_script==='object'&&!Array.isArray(raw.reels_script)){reelsScript={...(raw.reels_script as Record<string,unknown>)};if(!reelsScript.hook_0_5_sec)reelsScript.hook_0_5_sec='今天不要急著下結論，先看台積電有沒有真的帶隊。';if(!reelsScript.core_5_25_sec)reelsScript.core_5_25_sec=`盤前假設為${bias}，把握度${confScore}/100。關鍵在於：台積電是否強於大盤、半導體族群是否同步擴散。`;if(!reelsScript.risk_25_40_sec)reelsScript.risk_25_40_sec='最危險的不是看錯方向，是把盤前假設當成確定結論。';if(!reelsScript.watch_40_55_sec)reelsScript.watch_40_55_sec='今天先確認09:00開盤方向、09:30族群擴散、10:30資金持續性。三個時間點看三件事。';if(!reelsScript.cta_55_60_sec)reelsScript.cta_55_60_sec='這不是明牌，是盤前判斷節奏。追蹤Morning Alpha，每天07:30建立自己的盤前框架。';if(!reelsScript.full_script)reelsScript.full_script=[reelsScript.hook_0_5_sec,reelsScript.core_5_25_sec,reelsScript.risk_25_40_sec,reelsScript.watch_40_55_sec,reelsScript.cta_55_60_sec].filter(Boolean).join('\n\n')}else{reelsScript={hook_0_5_sec:'今天不要急著下結論，先看台積電有沒有真的帶隊。',core_5_25_sec:`盤前假設為${bias}，把握度${confScore}/100。關鍵在於：台積電是否強於大盤、半導體族群是否同步擴散。`,risk_25_40_sec:'最危險的不是看錯方向，是把盤前假設當成確定結論。',watch_40_55_sec:'今天先確認09:00開盤方向、09:30族群擴散、10:30資金持續性。三個時間點看三件事。',cta_55_60_sec:'這不是明牌，是盤前判斷節奏。追蹤Morning Alpha，每天07:30建立自己的盤前框架。',full_script:''};reelsScript.full_script=`${reelsScript.hook_0_5_sec}\n\n${reelsScript.core_5_25_sec}\n\n${reelsScript.risk_25_40_sec}\n\n${reelsScript.watch_40_55_sec}\n\n${reelsScript.cta_55_60_sec}`}let socialPost:Record<string,unknown>;if(raw.social_post&&typeof raw.social_post==='object'&&!Array.isArray(raw.social_post)){socialPost={...(raw.social_post as Record<string,unknown>)}}else{socialPost={title:isNonTrading?`Morning Alpha 最近交易日回顧：${bias}`:`今天市場${bias}，盤前有一件事比方向更重要`,three_points:[`盤前假設為${bias}，但關鍵不在方向，在傳導鏈有沒有成立`,'先確認台積電是否強於大盤，再觀察半導體族群是否同步','不要把盤前假設當結論，盤中09:00/09:30/10:30各有要確認的事'],risk_reminder:'最危險的不是看錯方向，是把盤前假設當成確定結論。',cta:'追蹤Morning Alpha，看每日盤前判斷節奏。',full_post:''};socialPost.full_post=`${socialPost.title}\n\n📊 三個重點\n1. ${(socialPost.three_points as string[])[0]}\n2. ${(socialPost.three_points as string[])[1]}\n3. ${(socialPost.three_points as string[])[2]}\n\n⚠️ ${socialPost.risk_reminder}\n\n—\n${socialPost.cta}`}let linePushCopy:Record<string,unknown>;if(raw.line_push_copy&&typeof raw.line_push_copy==='object'&&!Array.isArray(raw.line_push_copy)){linePushCopy={...(raw.line_push_copy as Record<string,unknown>)}}else{linePushCopy={title:isNonTrading?'Morning Alpha 最近交易日回顧':'Morning Alpha 今日盤前',market_bias:bias,confidence:`${confScore}/100`,one_sentence:String(raw.summary||`今日盤前假設為${bias}，先確認台積電方向再動作。`),do_not_do:'不要急著在開盤前決定方向',watch_point:'先看台積電、台指期、半導體族群',cta:'查看完整判讀'}}let doNotDoList:string[];if(Array.isArray(raw.do_not_do_list)&&raw.do_not_do_list.length>=3){doNotDoList=raw.do_not_do_list.map(String).slice(0,5)}else{doNotDoList=['不要只因為前一交易日美股走勢，就直接推論今日台股方向。','不要在台積電與台指期不同步時，急著追任何族群。','不要把開盤前15分鐘的強弱當成全天方向。','不要在資料不足時硬做方向判斷。','不要忽略收盤驗證，收盤才是檢查盤前劇本的關鍵。']}let invalidationConditions:Record<string,unknown>[];if(Array.isArray(raw.invalidation_conditions)&&raw.invalidation_conditions.length>=2){invalidationConditions=raw.invalidation_conditions.filter(x=>typeof x==='object'&&x!==null)as Record<string,unknown>[]}else{invalidationConditions=[{condition:'若台積電開盤方向與盤前假設相反',meaning:'代表盤前判斷需要降權或修正',required_adjustment:'將盤前假設降級為觀察模式'},{condition:'若台指期與加權指數不同步',meaning:'代表市場共識尚未形成',required_adjustment:'降低方向性判斷的權重'}]}let intradayTrackingPlan:Record<string,unknown>[];if(Array.isArray(raw.intraday_tracking_plan)&&raw.intraday_tracking_plan.length>=4){intradayTrackingPlan=raw.intraday_tracking_plan.filter(x=>typeof x==='object'&&x!==null)as Record<string,unknown>[]}else{intradayTrackingPlan=[{time:'09:00-09:15',question:'開盤方向是否與盤前假設一致？',what_to_watch:'台積電開盤方向、台指期是否支持現貨',interpretation:'若開盤方向與盤前劇本相反，需立即降權盤前判斷'},{time:'09:30',question:'權值股與族群是否同步？',what_to_watch:'半導體族群是否擴散',interpretation:'若權值股方向分歧，盤前假設可信度下降'},{time:'10:30',question:'資金是否擴散或只集中少數股票？',what_to_watch:'族群全面聯動還是只漲權值',interpretation:'若資金只集中1-2檔，代表市場信心不足'},{time:'13:00前',question:'是否接近收盤驗證條件？',what_to_watch:'開高後是否出現明顯賣壓',interpretation:'盤前假設是否被盤中驗證或需要修正'}]}let tomorrowWatchPoints:string[];if(Array.isArray(raw.tomorrow_watch_points)&&raw.tomorrow_watch_points.length>0){tomorrowWatchPoints=raw.tomorrow_watch_points.map(String).slice(0,5)}else{tomorrowWatchPoints=['收盤驗證後自動更新。']}let contentQualityFlags:Record<string,unknown>;if(raw.content_quality_flags&&typeof raw.content_quality_flags==='object'&&!Array.isArray(raw.content_quality_flags)){contentQualityFlags={...(raw.content_quality_flags as Record<string,unknown>)}}else{contentQualityFlags={has_member_research_note:!!raw.member_research_note,has_reels_script:!!raw.reels_script,has_social_post:!!raw.social_post,has_line_push_copy:!!raw.line_push_copy,has_do_not_do_list:doNotDoList.length>=3,has_invalidation_conditions:invalidationConditions.length>=2,has_intraday_tracking_plan:intradayTrackingPlan.length>=4,is_template_like:false,has_forbidden_words:false,has_fake_data_risk:false,quality_note:'AI生成，需人工抽檢。'}}const boolKeysCQF=['has_member_research_note','has_reels_script','has_social_post','has_line_push_copy','has_do_not_do_list','has_invalidation_conditions','has_intraday_tracking_plan','is_template_like','has_forbidden_words','has_fake_data_risk'];for(const k of boolKeysCQF){if(contentQualityFlags[k]==null)contentQualityFlags[k]=false}if(!contentQualityFlags.quality_note)contentQualityFlags.quality_note='AI生成，需人工抽檢。';
// V7.52: premium_value_summary
let premiumValueSummary:Record<string,unknown>;if(raw.premium_value_summary&&typeof raw.premium_value_summary==='object'&&!Array.isArray(raw.premium_value_summary)){premiumValueSummary={...(raw.premium_value_summary as Record<string,unknown>)}}else{premiumValueSummary={why_member_would_pay:'今天的盤前研究筆記提供了從隔夜數據到盤中追蹤到收盤驗證的完整框架，讓你知道不是只有方向，而是怎麼驗證、怎麼修正。',free_vs_member_gap:'免費版只給方向與一句提醒；會員版給你推理鏈、盤中時間表、失效條件與收盤回饋。',daily_return_reason:'第6段盤中追蹤計畫，告訴你09:00/09:30/10:30/13:00前各要看什麼。',strongest_member_value_today:`今日在「${bias}」狀態下，最重要的不是方向本身，而是知道什麼情況代表判斷失效。`,retention_hook:'明天收盤後回來看驗證結果，比對盤前假設與實際收盤。'}}
// V7.52: member_value_score
let memberValueScore:number;const rawMVS=Number(raw.member_value_score);if(!Number.isNaN(rawMVS)&&rawMVS>=0&&rawMVS<=100){memberValueScore=rawMVS}else{const hmn=!!raw.member_research_note;memberValueScore=hmn?Math.max(60,Math.min(90,confScore+10)):40}
// content_publish_gate
const hasFreeSummary=!!raw.free_summary,hasMemberNote=!!raw.member_research_note,hasReelsScript=!!raw.reels_script,hasSocialPost=!!raw.social_post,hasLinePush=!!raw.line_push_copy;const hasForbidden=(contentQualityFlags.has_forbidden_words===true),hasFakeRisk=(contentQualityFlags.has_fake_data_risk===true);const gateQS=hasMemberNote?Math.max(50,Math.min(100,85)):(hasFreeSummary?40:20);let gateOverallStatus:string;const gateReasons:string[]=[],gateBlocking:string[]=[];if(!hasMemberNote){gateOverallStatus='不建議公開';gateBlocking.push('缺少會員研究筆記')}else if(!hasFreeSummary){gateOverallStatus='需人工確認';gateReasons.push('免費摘要缺失')}else if(hasForbidden){gateOverallStatus='不建議公開';gateBlocking.push('內容含有禁詞')}else if(hasFakeRisk){gateOverallStatus='需人工確認';gateBlocking.push('內容可能有假資料風險')}else if(memberValueScore>=80&&gateQS>=80&&!hasForbidden&&!hasFakeRisk){gateOverallStatus='可公開';gateReasons.push('所有內容欄位完整，品質達標')}else if(memberValueScore>=70||gateQS>=60){gateOverallStatus='需人工確認';gateReasons.push('品質或會員價值偏低，建議人工檢查')}else{gateOverallStatus='不建議公開';gateBlocking.push('會員內容品質不足')}const cpg:Record<string,unknown>={overall_status:gateOverallStatus,free_summary_status:hasFreeSummary?'可用':'不可用',member_content_status:hasMemberNote?'完整':'缺失',reels_status:hasReelsScript?'可用':'不可用',line_push_status:hasLinePush?'可用':'不可用',quality_score:gateQS,member_value_score:memberValueScore,reasons:gateReasons,blocking_issues:gateBlocking,suggested_action:gateOverallStatus==='可公開'?'自動公開':gateOverallStatus==='需人工確認'?'人工審核':'等待重新產生'};let autoDecision:string,autoReason:string,ownerAction:boolean,urgency:string,ownerMsg:string;if(gateOverallStatus==='可公開'){autoDecision='自動公開';autoReason=`所有內容欄位完整，品質${gateQS}/100，會員價值${memberValueScore}/100。`;ownerAction=false;urgency='low';ownerMsg='今日內容已自動公開，無需站長處理。'}else if(gateOverallStatus==='需人工確認'){autoDecision='需人工確認';autoReason=`品質${gateQS}/100，會員價值${memberValueScore}/100。${gateReasons.join('；')}`;ownerAction=true;urgency='normal';ownerMsg='今日會員內容已產生但品質偏低或部分缺失，建議站長登入後台檢視。'}else{autoDecision='自動擋下';autoReason=gateBlocking.length>0?gateBlocking.join('；'):'會員內容未達公開標準';ownerAction=true;urgency='high';ownerMsg=`今日會員內容被自動擋下：${gateBlocking.join('、')||'品質不足'}。請重新觸發。`}const apd:Record<string,unknown>={decision:autoDecision,decided_at:new Date().toISOString(),reason:autoReason,requires_owner_action:ownerAction,urgency,owner_message:ownerMsg};let rvf:Record<string,unknown>;if(closeReview&&typeof closeReview==='object'){rvf={available:true,last_review_date:closeReview.review_date||closeReview.created_at||null,verification_result:closeReview.verification_result||closeReview.conclusion||'無資料',market_bias_accuracy:closeReview.market_bias_accuracy||closeReview.direction_match||null,key_insight:closeReview.key_insight||closeReview.summary||'無'}}else{rvf={available:false,reason:'尚無收盤驗證資料。系統會在使用者累積足夠交易日後自動產生收盤驗證回饋。'}}return{free_summary:freeSummary,member_research_note:memberResearchNote,reels_script:reelsScript,social_post:socialPost,line_push_copy:linePushCopy,do_not_do_list:doNotDoList,invalidation_conditions:invalidationConditions,intraday_tracking_plan:intradayTrackingPlan,tomorrow_watch_points:tomorrowWatchPoints,content_quality_flags:contentQualityFlags,premium_value_summary:premiumValueSummary,member_value_score:memberValueScore,content_publish_gate:cpg,auto_publish_decision:apd,recent_validation_feedback:rvf}}
function normalizeMemberReading(rawMR:unknown,bias:string,chains:Record<string,unknown>[]):{mr:Record<string,unknown>|null;repairedFields:string[]}{const rf:string[]=[];if(!rawMR||typeof rawMR!=='object'||Array.isArray(rawMR)){return{mr:fallbackMemberReading(bias,chains,{topNews:[],sectorCounts:{},mustMentionKeywords:[],topNewsTitles:[]},{baseScore:50,reasons:['備援'],riskReasons:[],details:{}}),repairedFields:['member_reading缺失']}}const mr=rawMR as Record<string,unknown>;if(typeof mr.core_script!=='string'||mr.core_script.length<10){mr.core_script=`今日${bias}，備援內容。`;rf.push('core_script')}if(!Array.isArray(mr.what_not_to_do)||mr.what_not_to_do.length===0){mr.what_not_to_do=['備援：觀察等待'];rf.push('what_not_to_do')}return{mr,repairedFields:rf}}
function normalizeReport(raw:Record<string,unknown>,fresh:FreshnessCheck,brief:NewsBrief,score:MarketDataScore,twCore:TWCoreStatus,mvpStatus:MVPStatus,source:string,qualityScore:number,externalWarnings:string[]=[],validationStatusOverride:string='fail',repairedFields:string[]=[],reportMode:string=REPORT_MODE_NORMAL,closeReview:Record<string,unknown>|null=null,generationMode:string='trading_day_fresh',isTradingDay:boolean=true,dataBasis:string='',latestTradingDate:string=''):Record<string,unknown>{const sn=(v:unknown,fb=0)=>{const n=Number(v);return Number.isNaN(n)?fb:n};const ss=(v:unknown,fb='')=>v==null?fb:String(v);const sa=(v:unknown,fb:string[]=[])=>{if(Array.isArray(v))return v.map(String).filter(Boolean);if(typeof v==='string'&&v.trim())return v.split(/[,，、;；\n]/).map(s=>s.trim()).filter(Boolean);return fb};const so=(v:unknown,fb:Record<string,unknown>[]=[])=>Array.isArray(v)?v.filter((x):x is Record<string,unknown>=>typeof x==='object'&&x!==null):fb;const sobj=(v:unknown,fb:Record<string,unknown>={})=>v&&typeof v==='object'&&!Array.isArray(v)?v as Record<string,unknown>:fb;const strat=sobj(raw.today_strategy||raw.ai_strategy_json);const tStrat={do:sa(strat.do),avoid:sa(strat.avoid)};const evts=so(raw.global_events_json).map(e=>({source:ss(e.source,'全球'),event:ss(e.event,'國際市場變動'),taiwanImpact:ss(e.taiwanImpact||e.taiwan_impact,'影響開盤'),beginnerTip:ss(e.beginnerTip||e.beginner_tip,'觀察'),relatedSector:ss(e.relatedSector||e.related_sector,'大盤')}));const risks=so(raw.risk_factors_json).map(r=>({title:ss(r.title||r.factor,'市場風險'),level:(String(r.level||'medium').toLowerCase().includes('high')?'high':String(r.level||'medium').toLowerCase().includes('low')?'low':'medium'),description:ss(r.description||r.impact,'留意波動')}));const ws=so(raw.watch_sectors_json||raw.watch_sectors).map(s=>({sector:ss(s.sector||s.name,'大盤'),direction:ss(s.direction||s.trend||'中性'),reason:ss(s.reason||s.why,'暫無')}));const wsFb=Object.entries(brief.sectorCounts).sort((a,b)=>b[1]-a[1]).slice(0,3).map(([sc])=>({sector:sc,direction:'觀察',reason:'新聞統計'}));const aiRawConfidence=Math.max(0,Math.min(100,sn(raw.confidence_score,50)));const rawSenti=sn(raw.sentiment_score,score.baseScore);const finalSenti=Math.max(Math.max(0,score.baseScore-10),Math.min(Math.min(100,score.baseScore+10),rawSenti));const isDSI=twCore.dataInsufficient||mvpStatus.mvpInsufficient;const cvResult=crossValidate(aiRawConfidence,finalSenti,score.baseScore,score.reasons,score.riskReasons,twCore.dataInsufficient,mvpStatus.mvpInsufficient);let effConf=cvResult.confidenceScore;if(isDSI){if(twCore.dataInsufficient&&effConf>70)effConf=70;if(mvpStatus.mvpInsufficient&&effConf>65)effConf=65}let fsSafe=finalSenti;if(fsSafe>=85&&effConf<50)fsSafe=Math.min(fsSafe,60);let sL:string;if(isDSI)sL=fsSafe<=20?'極度保守':fsSafe<=40?'保守':fsSafe<=60?'中性':fsSafe<=80?'偏多':'偏多';else{sL=ss(raw.sentiment_label,fsSafe<=20?'極度保守':fsSafe<=40?'保守':fsSafe<=60?'中性':fsSafe<=80?'偏多':'強勢偏多');if(!['極度保守','保守','中性','偏多','強勢偏多'].includes(sL))sL=fsSafe<=20?'極度保守':fsSafe<=40?'保守':fsSafe<=60?'中性':fsSafe<=80?'偏多':'強勢偏多'}const rawBias=ss(raw.market_bias,'');const compBias=classifyMarketBias(effConf,twCore.dataInsufficient,mvpStatus.mvpInsufficient);let finalBias=rawBias?normalizeMarketBiasToChinese(rawBias):compBias;if(source==='fallback')finalBias=compBias;const rawSum=ss(raw.summary,brief.topNews.length>0?`今日市場關注${brief.topNews[0].title.slice(0,30)}...`:'情報整理中...');const summary=sanitizeAIOutput(buildSummaryWithCap(rawSum,twCore.dataInsufficient,mvpStatus.mvpInsufficient),isDSI);const todaySummary=sanitizeAIOutput(buildSummaryWithCap(ss(raw.today_summary,rawSum),twCore.dataInsufficient,mvpStatus.mvpInsufficient),isDSI);const rawChains=Array.isArray(raw.overnight_impact_chains)?(raw.overnight_impact_chains as Record<string,unknown>[]):[];const impactChains=rawChains.filter(c=>typeof c==='object'&&c!==null);const isNonTrading=reportMode===REPORT_MODE_NON_TRADING;const newAIFields=normalizeNewAIFields(raw,finalBias,effConf,isDSI,isNonTrading,impactChains,closeReview,isTradingDay,dataBasis,latestTradingDate);const freshnessNote=!isTradingDay?'非交易日，使用最近交易日資料':'';return{today_quote:ss(raw.today_quote,'市場每天都有訊號，但你的情緒不該是其中之一。'),summary,market_bias:finalBias,confidence_score:effConf,confidence_label:ss(raw.confidence_label,effConf>=90?'高度信心':effConf>=75?'中高信心':effConf>=50?'尚可':'偏低'),sentiment_score:fsSafe,sentiment_label:sL,sentiment_reason:ss(raw.sentiment_reason,score.reasons.join('、')),risk_reason:ss(raw.risk_reason,score.riskReasons.join('、')),today_strategy:tStrat,can_watch:sa(raw.can_watch,[]),avoid_today:sa(raw.avoid_today,['追高','重壓']),fear_greed:Math.max(0,Math.min(100,sn(raw.fear_greed,50))),fear_greed_summary:ss(raw.fear_greed_summary,'中等'),vix:sn(raw.vix,18),vix_summary:ss(raw.vix_summary,'波動正常'),risk_factors_json:risks.length?risks:[{title:'市場波動',level:'medium',description:'分批觀察'}],watch_sectors_json:ws.length?ws:(wsFb.length?wsFb:[{sector:'大盤',direction:'中性',reason:'觀望'}]),focus_stock_json:so(raw.focus_stock_json).map(f=>({group:ss(f.group||f.sector,'大盤'),direction:ss(f.direction,'觀察'),reason:ss(f.reason,'觀望')})),tomorrow_watch_json:so(raw.tomorrow_watch_json).map(t=>({name:ss(t.name,'大盤'),reason:ss(t.reason,'觀察')})),global_events_json:evts.length?evts:[{source:'全球',event:'國際變動',taiwanImpact:'影響開盤',beginnerTip:'觀察',relatedSector:'大盤'}],ai_strategy_json:{version:VERSION,report_mode:reportMode,generation_mode:generationMode,source,validation_status:validationStatusOverride,impact_chain_source:source,member_reading_source:source,quality_score:qualityScore,quality_warnings:externalWarnings,repaired_fields:repairedFields,generated_at:new Date().toISOString(),is_trading_day:isTradingDay,data_basis:dataBasis||'latest_trading_day',latest_trading_date:latestTradingDate||'',freshness_note:freshnessNote,overnight_impact_chains:impactChains,member_reading:fallbackMemberReading(finalBias,impactChains,brief,score),free_summary:newAIFields.free_summary,member_research_note:newAIFields.member_research_note,reels_script:newAIFields.reels_script,social_post:newAIFields.social_post,line_push_copy:newAIFields.line_push_copy,do_not_do_list:newAIFields.do_not_do_list,invalidation_conditions:newAIFields.invalidation_conditions,intraday_tracking_plan:newAIFields.intraday_tracking_plan,tomorrow_watch_points:newAIFields.tomorrow_watch_points,content_quality_flags:newAIFields.content_quality_flags,premium_value_summary:newAIFields.premium_value_summary,member_value_score:newAIFields.member_value_score,content_publish_gate:newAIFields.content_publish_gate,auto_publish_decision:newAIFields.auto_publish_decision,recent_validation_feedback:newAIFields.recent_validation_feedback,data_health:{market_data_fresh:fresh.marketDataFresh,news_fresh:fresh.newsFresh,data_count:fresh.dataCount,news_count:fresh.newsCount,tw_core_insufficient:twCore.dataInsufficient,mvp_insufficient:mvpStatus.mvpInsufficient,score_base:score.baseScore},legacy:{conservative:ss(sobj(raw.ai_strategy_json).conservative,'先觀察'),aggressive:ss(sobj(raw.ai_strategy_json).aggressive,'留意熱門'),overall_advice:ss(sobj(raw.ai_strategy_json).overall_advice,'建議觀望'),risk_warning:ss(sobj(raw.ai_strategy_json).risk_warning,'投資有風險')}}as unknown as Record<string,unknown>,important_news_json:(so(raw.important_news_json).length?so(raw.important_news_json):brief.topNews.slice(0,3).map(n=>({title:n.title,summary:n.taiwan_impact_summary||'關注',impact:n.taiwan_impact_summary||'待觀察',sectors:Array.isArray(n.related_sectors)?n.related_sectors:[]}))).map(n=>({title:ss(n.title,'新聞'),summary:ss(n.summary,'關注'),impact:ss(n.impact,'待觀察'),sectors:sa((n as Record<string,unknown>).sectors)})),yesterday_summary:ss(raw.yesterday_summary,'昨日正常波動'),today_summary:todaySummary,ai_psychology:ss(raw.ai_psychology,'別因情緒做錯決定。'),ai_retail_reminder:ss(raw.ai_retail_reminder,'冷靜觀察比衝動進場更重要。'),ai_confidence_reason:ss(raw.ai_confidence_reason,'建議觀察後再決定。'),script_valid_conditions:sa(raw.script_valid_conditions,['主流沒有快速退潮']),script_invalid_conditions:sa(raw.script_invalid_conditions,['開高走低']),today_key_signal:ss(raw.today_key_signal,'觀察主流延續'),today_key_signal_reason:ss(raw.today_key_signal_reason,'確認方向'),intraday_checkpoints:so(raw.intraday_checkpoints,[{time:'09:00',check:'開盤',why:'不急'},{time:'10:30',check:'主流',why:'確認'}]),overnight_impact_chains:impactChains,impact_chain_quality_score:qualityScore,_member_reading:fallbackMemberReading(finalBias,impactChains,brief,score),_member_reading_source:source,_quality_warnings:externalWarnings,_repaired_fields:repairedFields,_cross_validated:cvResult.recalibrated,_cross_validation_reason:cvResult.reason||'',_tw_core_insufficient:twCore.dataInsufficient,_mvp_insufficient:mvpStatus.mvpInsufficient,member_reading_source:source,quality_warnings:externalWarnings,repaired_fields:repairedFields}as unknown as Record<string,unknown>}
function buildPayload(report:Record<string,unknown>):Record<string,unknown>{const keys=['summary','market_bias','confidence_score','confidence_label','today_quote','today_strategy','can_watch','avoid_today','fear_greed','fear_greed_summary','vix','vix_summary','risk_factors_json','watch_sectors_json','focus_stock_json','tomorrow_watch_json','global_events_json','ai_strategy_json','important_news_json','yesterday_summary','today_summary','ai_psychology','ai_retail_reminder','ai_confidence_reason','sentiment_score','sentiment_label','sentiment_reason','risk_reason'];const p:Record<string,unknown>={};for(const k of keys){if(k in report)p[k]=report[k]}if(!p.ai_strategy_json||typeof p.ai_strategy_json!=='object')p.ai_strategy_json={};const ai={...(p.ai_strategy_json as Record<string,unknown>)};if(Array.isArray(report.script_valid_conditions))ai.script_valid_conditions=report.script_valid_conditions;if(Array.isArray(report.script_invalid_conditions))ai.script_invalid_conditions=report.script_invalid_conditions;if(report.today_key_signal)ai.today_key_signal=report.today_key_signal;if(report.today_key_signal_reason)ai.today_key_signal_reason=report.today_key_signal_reason;if(Array.isArray(report.intraday_checkpoints))ai.intraday_checkpoints=report.intraday_checkpoints;p.ai_strategy_json=ai;return p}
function buildFallbackReport(md:MarketIndicator[],nd:MarketNewsItem[],brief:NewsBrief,score:MarketDataScore,twCore:TWCoreStatus,mvpStatus:MVPStatus,yesterday:Record<string,unknown>|null,fresh:FreshnessCheck,reportMode:string,closeReview:Record<string,unknown>|null=null,generationMode:string='trading_day_fresh',isTradingDay:boolean=true,dataBasis:string='',latestTradingDate:string=''):Record<string,unknown>{const tn=brief.topNews.slice(0,3);const ts=Object.entries(brief.sectorCounts).sort((a,b)=>b[1]-a[1]).slice(0,3);const fb=classifyMarketBias(score.baseScore,twCore.dataInsufficient,mvpStatus.mvpInsufficient);const isDSI=twCore.dataInsufficient||mvpStatus.mvpInsufficient;const isNonTrading=reportMode===REPORT_MODE_NON_TRADING;const d=isDSI?'資料不足。':'備援內容。';const confScore=Math.min(score.baseScore,50);const fcs:Record<string,unknown>[]=ts.map(([sec,cnt])=>({chain_title:`${sec}族群需觀察盤中方向`,direction:'neutral',source_events:tn.slice(0,2).map(n=>({title:n.title||'新聞',source:n.source||'未知',published_at:n.published_at||new Date().toISOString(),why_relevant:`可能影響${sec}相關個股`})),market_evidence:[{symbol:'TAIEX',change_percent:'0.0',interpretation:'台股加權方向未明'}],causal_path:[`${sec}相關新聞增加`,'市場關注度上升','需觀察開盤資金'],why_it_matters:`${sec}族群在新聞提及${cnt}次。`,taiwan_sector_impact:`${sec}族群可能受隔夜消息影響。`,affected_sectors:[{sector_name:sec,impact_direction:'watch',reason:`新聞提及${cnt}次`}],representative_stocks:[{symbol:'2330',name:'台積電',role:'權值龍頭',reason:'台積電是權值股核心',watch_condition:'觀察是否站穩平盤'},{symbol:'2454',name:'聯發科',role:'IC設計',reason:'聯發科反映科技情緒',watch_condition:'觀察是否同步表態'},{symbol:'2382',name:'廣達',role:'AI伺服器',reason:'廣達反映AI需求',watch_condition:'觀察資金承接'}],intraday_watch_points:[{time:'09:15',watch:'觀察權值股方向',bullish_confirm:'開高且方向一致',bearish_confirm:'開低且無買盤'}],confirm_conditions:['方向一致'],invalid_conditions:['方向分歧量縮'],plain_language_summary:`${d}先觀察${sec}族群表現。`,_fallback:true}));const fbNewFields=normalizeNewAIFields({},fb,confScore,isDSI,isNonTrading,fcs,closeReview,isTradingDay,dataBasis,latestTradingDate);return{market_bias:fb,summary:`${d}市場關注${tn.map(n=>n.title?.slice(0,20)).join('、')}`,confidence_score:confScore,sentiment_score:Math.min(score.baseScore,50),sentiment_label:'中性',sentiment_reason:score.reasons.join('、'),risk_reason:score.riskReasons.join('、'),today_quote:'備援模式：以盤中數據為準。',today_strategy:{do:['觀察'],avoid:['衝動']},can_watch:ts.map(([s])=>s),avoid_today:['追高','重壓'],overnight_impact_chains:fcs,member_reading:{core_script:`${d}今日建議以觀察為主。`,what_not_to_do:['不要在方向不明時進場','不要根據不完整資訊做決定','不要急於追價'],watchlist_logic:['等待開盤確認','觀察權值股','注意族群輪動'],closing_validation_focus:['收盤確認方向','確認明日資訊']},member_reading_source:'fallback',quality_warnings:['fallback'],fear_greed:50,vix:18,ai_strategy_json:{version:VERSION,report_mode:reportMode,generation_mode:generationMode,source:'fallback',validation_status:'fail',impact_chain_source:'fallback',member_reading_source:'fallback',quality_score:confScore,quality_warnings:['fallback'],generated_at:new Date().toISOString(),is_trading_day:isTradingDay,data_basis:dataBasis||'latest_trading_day',latest_trading_date:latestTradingDate||'',freshness_note:!isTradingDay?'非交易日備援，使用最近交易日資料':'',overnight_impact_chains:fcs,member_reading:{core_script:`${d}今日建議以觀察為主。`},free_summary:fbNewFields.free_summary,member_research_note:fbNewFields.member_research_note,reels_script:fbNewFields.reels_script,social_post:fbNewFields.social_post,line_push_copy:fbNewFields.line_push_copy,do_not_do_list:fbNewFields.do_not_do_list,invalidation_conditions:fbNewFields.invalidation_conditions,intraday_tracking_plan:fbNewFields.intraday_tracking_plan,tomorrow_watch_points:fbNewFields.tomorrow_watch_points,content_quality_flags:fbNewFields.content_quality_flags,premium_value_summary:fbNewFields.premium_value_summary,member_value_score:fbNewFields.member_value_score,content_publish_gate:fbNewFields.content_publish_gate,auto_publish_decision:fbNewFields.auto_publish_decision,recent_validation_feedback:fbNewFields.recent_validation_feedback,data_health:{market_data_fresh:fresh.marketDataFresh,news_fresh:fresh.newsFresh,data_count:fresh.dataCount,news_count:fresh.newsCount,tw_core_insufficient:twCore.dataInsufficient,mvp_insufficient:mvpStatus.mvpInsufficient,score_base:score.baseScore},legacy:{}}as unknown as Record<string,unknown>,important_news_json:tn.map(n=>({title:n.title||'新聞',summary:n.taiwan_impact_summary||'關注',impact:n.taiwan_impact_summary||'待觀察',sectors:Array.isArray(n.related_sectors)?n.related_sectors:[]})),yesterday_summary:yesterday?`昨日：${yesterday.market_bias||'無'}`:'無昨日',today_summary:d,ai_psychology:'保持冷靜',ai_retail_reminder:'備援僅供參考',ai_confidence_reason:'AI未成功產生判讀',script_valid_conditions:['開盤方向明確'],script_invalid_conditions:['方向不明'],today_key_signal:'觀察',today_key_signal_reason:'備援',watch_sectors_json:ts.map(([s])=>({sector:s,direction:'觀察',reason:'備援'})),risk_factors_json:[{title:'備援',level:'medium',description:'AI未成功'}],global_events_json:[{source:'備援',event:'備援',taiwanImpact:'觀察',beginnerTip:'觀察',relatedSector:'大盤'}],focus_stock_json:[{group:'大盤',direction:'觀察',reason:'備援'}],tomorrow_watch_json:[{name:'大盤',reason:'備援'}],intraday_checkpoints:[{time:'09:00',check:'開盤',why:'確認'}],correction_guide:'備援模式',confidence_label:'偏低',impact_chain_quality_score:confScore}}