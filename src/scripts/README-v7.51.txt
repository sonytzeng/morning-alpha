COMMENT: This file references parts 1-3 in src/scripts/v7.51-function-part{1,2,3}.txt
Please open all three files in Readdy and combine them in the Supabase Dashboard Edge Function editor:
1. Open part1.txt → select all → copy
2. Open part2.txt → select all → copy (paste AFTER part1 content)  
3. Open part3.txt → select all → copy (paste AFTER part2 content)
4. Click Deploy in Supabase Dashboard

The complete function is ~25K characters - too large for the deploy tool but well within Supabase Dashboard's limit.